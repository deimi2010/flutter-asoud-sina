import 'package:asood/main.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('application root can be created', () {
    expect(const Asood(), isA<Widget>());
  });
}
